import React from 'react';

const RepoCard = ({ repo }) => {
  if (!repo) return null;

  return (
    <div className="repo-card">
      {/* Repo details implementation */}
    </div>
  );
};

export default RepoCard;
